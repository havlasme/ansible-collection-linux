---
- name: "prepare"
  hosts: "all"

  tasks:
  - name: "install required package"
    ansible.builtin.package:
      name: "locales"
  - name: "prepare locale state present -> absent"
    community.general.locale_gen:
      name: "sk_SK.UTF-8"
