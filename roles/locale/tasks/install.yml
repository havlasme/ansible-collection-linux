---
- name: "(linux.locale) include os specific variables"
  ansible.builtin.include_vars: "{{ ansible_os_family | lower }}.yml"

- name: "(linux.locale) install required package"
  ansible.builtin.package:
    name: "{{ locale_package }}"
    state: "present"
  when: "linux_locale | length > 0"
