---
- name: "verify"
  hosts: "all"
  gather_facts: false

  tasks:
  - name: "assert that the previously absent group is present"
    ansible.builtin.shell: "getent group absent-to-present"
    changed_when: false
