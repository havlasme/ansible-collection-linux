---
- name: "verify"
  hosts: "all"
  gather_facts: false

  tasks:
  - name: "assert that the previously present user is absent"
    ansible.builtin.shell: "! getent passwd present-to-absent"
    changed_when: false
