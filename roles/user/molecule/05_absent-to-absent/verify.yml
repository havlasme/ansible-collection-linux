---
- name: "verify"
  hosts: "all"
  gather_facts: false

  tasks:
  - name: "assert that the previously absent user is absent"
    ansible.builtin.shell: "! getent passwd absent-to-absent"
    changed_when: false
