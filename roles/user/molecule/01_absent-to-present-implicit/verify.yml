---
- name: "verify"
  hosts: "all"
  gather_facts: false

  tasks:
  - name: "assert that the previously absent user is present"
    ansible.builtin.shell: "getent passwd absent-to-present"
    changed_when: false
