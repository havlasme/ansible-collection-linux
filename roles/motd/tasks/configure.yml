---
- name: "linux | motd | set the motd"
  ansible.builtin.template:
    dest: "{{ linux_motd }}"
    src: "{{ linux_motd_template }}"
    owner: "root"
    group: "root"
    mode: "0644"
  when:
  - "linux_motd_template is defined"
