---
- name: "(linux.motd) set the local motd"
  ansible.builtin.template:
    dest: "{{ linux_motd | mandatory }}"
    src: "{{ linux_motd_tmpl | mandatory }}"
    owner: "root"
    group: "root"
    mode: "0644"
  when: "linux_motd_tmpl is defined"
