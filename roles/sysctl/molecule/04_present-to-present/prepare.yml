---
- name: "prepare"
  hosts: "all"

  tasks:
  - name: "install required package"
    ansible.builtin.package:
      name: "procps"
      state: "present"
  - name: "prepare sysctl config state absent -> present"
    ansible.posix.sysctl:
      name: "vm.swappiness"
      value: "10"
      reload: "no"
